# GPU-specific dependencies for faster-whisper provider
# These are only installed when ENABLE_GPU=true during Docker build

# faster-whisper with CTranslate2 backend
faster-whisper>=1.0.0

# PyTorch for Silero VAD (CPU version is sufficient for VAD)
# The ONNX backend handles GPU inference
torch>=2.0.0
torchaudio>=2.0.0

# CTranslate2 - CUDA-optimized inference engine
ctranslate2>=4.0.0
