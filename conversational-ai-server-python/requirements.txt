# Core LiveKit integration - correct package names
livekit>=0.9.0
livekit-api>=0.5.0

# Real-time STT with local/opensource models (container-compatible)
faster-whisper>=1.0.0  # Optimized Whisper for CPU/GPU, container-friendly
silero-vad>=5.0  # Silero VAD for voice activity detection
torch>=2.0.0  # Required for Silero VAD
torchaudio>=2.0.0  # Required for audio processing

# LangChain for agent orchestration
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.1.0

# OpenAI API
openai>=1.0.0

# Core Python packages
pydantic>=2.0.0

# JSON handling and utilities
typing-extensions>=4.0.0

# Optional: For development and debugging
python-dotenv>=1.0.0

# Audio transcription dependencies
sherpa-onnx>=1.12.0
numpy>=1.24.0

# Text-to-Speech dependencies
pyttsx3>=2.90
edge-tts>=7.0.0
pydub>=0.25.1

# Kokoro TTS dependencies
kokoro-onnx>=0.4.9
soundfile>=0.12.1
# Note: onnxruntime is installed conditionally in Dockerfile based on GPU support
#   - Mac builds: onnxruntime (CPU-only)
#   - Linux GPU builds: onnxruntime-gpu (CUDA-enabled with CPU fallback)
scipy>=1.11.0

# ElevenLabs TTS dependencies (optional - install only if using ElevenLabs)
# For ElevenLabs support
elevenlabs>=1.0.0
websockets>=11.0.0

# Local LLM providers (optional - install only what you need)
# For Ollama support
requests>=2.25.0

# For llama.cpp support (uncomment if using)
# llama-cpp-python>=0.2.0

# For Hugging Face Transformers support (uncomment if using)
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0